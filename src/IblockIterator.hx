
class IblockIterator extends CDBResultIterator {
    /*
    override public function next() {
        super.next();
        //current['_API_PROPERTIES'] = '/bitrix/tools/rodzeta.siteoptions/?entity=iblock_properties&id=' + current['ID'];
        //current['_API_SECTIONS'] = '/bitrix/tools/rodzeta.siteoptions/?entity=iblock_sections&id=' + current['ID'];
        //current['_API_ELEMENTS'] = '/bitrix/tools/rodzeta.siteoptions/?entity=iblock_elements&id=' + current['ID'];
        return current;
    }
    */
}
